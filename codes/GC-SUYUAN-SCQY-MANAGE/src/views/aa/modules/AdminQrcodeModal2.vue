<template>
  <j-modal
    :title="title"
    :width="width"
    :visible="visible"
    switchFullscreen
    @ok="handleOk"
    :okButtonProps="{ class:{'jee-hidden': disableSubmit} }"
    @cancel="handleCancel"
    cancelText="关闭">
    <admin-qrcode-form2 ref="realForm" @ok="submitCallback" :disabled="disableSubmit"></admin-qrcode-form2>
  </j-modal>
</template>

<script>

  import AdminQrcodeForm2 from './AdminQrcodeForm2'


  import Print from 'vue-print-nb'
  export default {
    name: 'AdminQrcodeModal2',
    components: {
      AdminQrcodeForm2
    },
    data () {
      return {
        title:'',
        width:800,
        visible: false,
        disableSubmit: false
      }
    },
    methods: {
      add () {
        this.visible=true
        this.$nextTick(()=>{
          this.$refs.realForm.add();
        })
      },
      edit (record) {
        this.visible=true
        this.$nextTick(()=>{
          this.$refs.realForm.edit(record);
        })
      },
      close () {
        this.$emit('close');
        this.visible = false;
      },
      handleOk () {
        console.log(this.$refs.realForm.model.dayinNum)
        this.$refs.realForm.submitForm();
      },
      submitCallback(){
        this.$emit('ok');
        this.visible = false;
      },
      handleCancel () {
        this.close()
      }
    }
  }
</script>